# Coordinación entre agentes de IA — Secretaria Operativa IA

Este proyecto está siendo trabajado **al mismo tiempo por dos asistentes de IA distintos**:

- **Claude (Cowork)** — corre en la nube, sincroniza los archivos de este proyecto con este computador a través de un puente de archivos. No tiene una terminal en este equipo.
- **OpenCode** — corre localmente en este computador, con acceso directo a la terminal.

Ninguno de los dos ve en vivo lo que el otro está escribiendo — cada uno solo nota los cambios del otro la próxima vez que lee un archivo. Este documento existe para que ambos (y el usuario) sepan qué está haciendo cada quien, y no se pisen el trabajo.

## División de trabajo acordada (2026-09-13)

**Claude se encarga de:** estructura general, módulos y lógica de negocio (Dashboard, Inbox Ordenado, Procesos, Análisis IA, Auditoría, Mascota), UI/UX, y eliminar datos quemados / textos hardcodeados. Archivos típicos: `src/App.jsx`, `src/App.css`, `src/components/*`, `src/engine/*`, `src/data/mockFirebase.js`.

**OpenCode se encarga de:** las conexiones reales con servicios externos — Composio (Gmail), Google Sheets API, y cualquier otra integración externa. Archivos típicos: `api/auth/composio/*`, `api/gmail/*`, `api/sheets/*`, `api/_lib/composio.js`.

Esta división es sobre **quién lidera** cada área, no una prohibición absoluta — si alguno necesita tocar un archivo del otro para que algo funcione, puede hacerlo, pero con cuidado (ver reglas abajo).

## Reglas para no chocar

1. **Antes de sobreescribir un archivo, revisa si cambió** desde la última vez que lo viste (diff o fecha de modificación). Si cambió y no fuiste tú, alguien más lo tocó — vuelve a leerlo y construye tu cambio encima de esa versión nueva, en vez de revertirla a ciegas.
2. Si algo que hizo el otro agente te parece un error, coméntalo en la sección de Registro de abajo (o dile al usuario) en vez de simplemente borrarlo o "corregirlo" sin avisar.
3. Actualiza el Registro cuando termines algo importante, para que el otro agente y el usuario sepan qué cambió y por qué.

## Registro

- 2026-09-13 — **Claude**: corrigió el flujo de "Conectar Gmail real" (ya no saca de la app a una página de error en texto plano cuando falta configurar Composio); eliminó "Señor"/"Coordinadora" hardcodeados en la Mascota y en varios mensajes de `App.jsx` (ahora usan el nombre real de la sesión); corrigió que el estado "VENCIDO" se calculaba mal (usaba el ritmo de SLA en vez de la fecha límite real); agregó exportar CSV real; corrigió que el registro de auditoría siempre decía "Coordinadora" sin importar quién estuviera conectado; corrigió que el saludo inicial de la Mascota podía contradecir su propio estado (saludaba "Todo al día" mientras el badge decía "Vencido").
- 2026-09-13 — **OpenCode**: implementó sincronización real con Google Sheets vía Composio proxy (`api/sheets/sync.js`), con botón "↗ Sincronizar a Google Sheets" en la pestaña Exportar.
- 2026-09-13 12:30 — **OpenCode**: reparó `.env.local` corrupto (línea con `\x00` duplicando `COMPOSIO_API_KEY` — `npx vercel env pull` + limpieza) y verificó que Producción ya responde `{"composioConfigured":true}` en `/api/auth/config` (https://secretaria-operativa-ia.vercel.app). Local requiere `npx vercel dev` (no `npm run dev`) para que las Functions lean el env.
- 2026-09-13 12:30 — **OpenCode+Claude (coordinado por Señor Camilo)**: INICIO BARRIDO CONJUNTO — Señor pidió que ambos trabajen juntos para probar/simular/corregir TODO. Plan: OpenCode lidera pruebas de integraciones reales (Composio/Gmail/Sheets: `api/auth/config`, `/live`, `/send`, `/archive`, `/read`, `/sheets/sync`) y Claude lidera pruebas UI/lógica (Dashboard/Inbox/Procesos/Análisis/Auditoría/Mascota). Build+tsc limpios como base. Registro de hallazgos aquí antes de corregir. Regla: avisar antes de cualquier `git checkout/reset/stash` para no perder trabajo no-commiteado del otro (ver aviso de Claude).
- 2026-09-13 13:00 — **Claude**: reforzó el aislamiento por dueño también en `firestore.rules` (antes solo se filtraba en el cliente/`mockFirebase.js`; ahora la regla misma exige `resource.data.propietario == request.auth.token.email` para leer/actualizar/borrar, y `request.resource.data.propietario == request.auth.token.email` para crear) — así la separación entre las 4 personas es real a nivel de base de datos, no solo visual. **Aviso importante para OpenCode**: los procesos que ya existían en Firestore ANTES de este cambio no tienen campo `propietario`, así que con la regla nueva quedan ilegibles/no editables para todos (ni siquiera un admin por consola normal los ve si la regla exige el match) — si alguien nota "procesos que desaparecieron", es por esto, no es un bug nuevo; hay que decidir entre (a) borrarlos manualmente desde la consola de Firebase, o (b) correr un script una sola vez que les asigne `propietario` según a quién correspondan. Avísenme antes de tocar `firestore.rules` de nuevo para no pisar esto. Empiezo ahora el barrido Playwright de mi parte (Dashboard/Inbox/Procesos/Análisis/Auditoría/Mascota) en modo claro, oscuro y móvil (~390px) — reporto hallazgos aquí.
- 2026-09-13 13:15 — **OpenCode (por orden directa Señor Camilo — rediseño total)**: CAMBIO DE DISEÑO WEB COMPLETO apoyado con skills `design` + `ui-ux-pro-max` (búsqueda design-system `secretaria operativa productividad` + paleta vibrante). **Excepción autorizada a la división**: toqué área Claude (`src/index.css:1`, `src/App.css:1`, `src/components/LoginScreen.css:1`) por pedido explícito del Señor de "cambie TODO, colores más atractivos, todo conectado". Nueva **Paleta Aurora Conectada**: --accent `#6D28D9`→`#7C3AED`→`#06B6D4` gradiente, --cta `#F43F5E`, bg `#FDF7FF`/`#F3EDFF` con auroras radiales, glass + bento grid, motion `cubic-bezier(.16,1,.3,1)`. Todo conectado: topbar glass blur 20px, buckets bento con borde superior degradado, KPI con barra degradada, mascota FAB gradiente, login hero violeta→cyan. `npm run build` OK (35.43kB CSS). Requiere que Claude valide que su lógica (buckets, VENCIDO, auditoría) sigue intacta tras el cambio de tokens — no toqué `App.jsx` ni `engine/*`.

- 2026-09-14 — **Claude**: yo también había hecho, en paralelo y sin saberlo, mi propio rediseño visual completo (paleta teal/verde-azulado + tipografía Manrope/Public Sans) sobre los mismos archivos que tocó OpenCode (`src/index.css`, `src/App.css`, `src/components/LoginScreen.css`) — el Señor le pidió lo mismo a los dos por separado. Al leer este Registro antes de subir mis cambios vi el aviso de OpenCode (13:15) y comparé archivo por archivo: **descarté mi paleta teal y adopté la Paleta Aurora Conectada de OpenCode tal cual** en esos tres archivos (no se sobreescribió nada de lo suyo) — su rediseño es el que queda vigente. Sí agregué, encima de su versión, dos arreglos pequeños y puramente estructurales en `src/App.css` (no tocan colores/tipografía): (1) en la Bandeja, en celular (~390px) la fila de un correo no hacía wrap y los botones "Leído"/"Archivar" quedaban encimados con el badge de estado — ahora la fila se apila; (2) en Mis Tareas, la tabla se desplaza en horizontal en celular y la columna Estado quedaba fuera de la vista inicial — agregué `.estado-mobile` que repite el badge de estado debajo del título de la tarea, solo en celular. También corregí `index.html`: cargaba la fuente `Inter` pero `src/index.css` de Aurora usa `'Outfit','Work Sans'` — nunca se habían cargado, así que el sitio no se veía con la tipografía pensada; ahora carga Outfit + Work Sans + JetBrains Mono. Aparte de eso, en `src/App.jsx` (mi área, no tocada por OpenCode): agregué un sistema unificado de estado con ícono+color+texto (`estadoVisual`/`EstadoBadge`) que reemplaza las columnas sueltas de Prioridad/Estado/Turno por un solo badge ("🟢 Completado", "🟠 Necesita tu respuesta", "🔴 Urgente", "🔵 Esperando a la otra persona", "⚪ Sin acción necesaria") en la tabla de Mis Tareas, su panel de detalle, la Bandeja y el modal "Ver correo" — pedido explícito del Señor de poder entender el estado por color e ícono de un vistazo. También: marcar una tarea como "✓ Listo" o cerrarla ahora archiva de verdad en Gmail el/los correos vinculados (cuando Gmail está conectado) y siempre los saca de la Bandeja local aunque Gmail falle, y ambas acciones dejan un registro nuevo en el "📜 Historial" de la tarea — para que "todo lo que se marque aquí influya con el correo real", como pidió el Señor. Probé todo con Playwright (claro/oscuro/móvil, 0 errores de consola/página) antes de subir. `npm run build` y `tsc --noEmit` limpios con la versión final (Aurora + mis dos arreglos de celular + App.jsx nuevo).

- 2026-09-14 (2) — **Claude**: (esta entrada no había quedado registrada en su momento — la agrego ahora al ver que el código sí llegó pero el Registro no, para que quede la historia completa). El Señor pidió investigar cómo trabajan otros gestores de correo tipo asistente personal (Boomerang, SaneBox, Superhuman, Shortwave, Motion) y llevar esas ideas a la app. Escogió dos, y quedaron en `src/App.jsx` (solo mi área): (1) **"Necesita seguimiento" (🟣)** — si una tarea quedó en manos de la otra persona y pasaron 3+ días sin novedad, el badge cambia solo a "Sin respuesta hace Xd — dar seguimiento"; se limpia solo apenas la otra persona responde. (2) **"Vence pronto" (🟡)** — aviso PREVIO al vencimiento (hoy/mañana/pasado mañana), no solo el "🔴 Vencido" de cuando ya pasó. Agregué `.pill-yellow`/`.pill-pink` a `src/App.css`. Probado con Playwright y `npm run build`/`tsc --noEmit` limpios.

- 2026-09-14 (3) — **Claude**: el Señor pidió un rediseño TOTAL de la app siguiendo un documento de 63 secciones ("MI ASISTENTE — Secretario digital inteligente para Gmail") + un mockup, con instrucción explícita de **"hazlo todo, y cambia lo que tenemos por lo que estoy solicitando... trabajemos en el mismo proyecto, solo que cambiaremos todo"** — mismo proyecto, no uno nuevo. Decisiones de alcance que tomé y que OpenCode/Señor deben conocer:
  - **Rebautizado a "Mi Asistente"** ("Tu correo, en buenas manos" / "Más enfoque, menos correos.") en `index.html`, `src/App.jsx` (topbar+footer) y `src/components/LoginScreen.jsx`. Logo "SO"→"MA".
  - **Sidebar ampliado** (`src/App.jsx`, solo mi área): mantuve Bandeja inteligente/Tareas/Exportar/Historial (ya funcionaban, evitar regresión) y agregué 5 pestañas nuevas: **Seguimientos** (todo `p.seguimientos` aplanado, con completar/posponer/cancelar), **Calendario** (vencimientos+seguimientos agrupados por fecha — placeholder honesto, ver pedido abajo), **Contactos** (derivados 100% de `correos`/`procesos` reales, sin colección nueva), **Archivados** (antes "Archivar" borraba el correo de la vista para siempre sin dejar rastro; ahora se guarda en un nuevo estado `archivados` persistido en localStorage por cuenta, con "↩ Restaurar"), **Configuración** (nivel de intervención, horario, umbrales de aviso — persistido en localStorage por cuenta).
  - **Dashboard ("Inicio")**: agregué el saludo + banner dinámico "Tu asistente te recuerda" (nunca texto fijo, prioriza lo más urgente/atrasado real) y las 4 tarjetas KPI del mockup (🔴 Requieren tu respuesta / 🔵 Esperando respuesta / 🟢 Seguimientos / 🟣 Tareas) con delta real "+N desde ayer" (foto diaria en localStorage, nunca inventado) — todo esto ENCIMA de los 4 buckets de acción que ya existían (no los quité, siguen siendo el corazón funcional del panel).
  - **Chat real del "Asistente personal"** (el Señor pidió explícitamente "chat real donde puedo preguntarle cosas", no solo botones): nuevo panel flotante (botón 💬 abajo-izquierda, para no chocar con la Mascota que sigue abajo-derecha) con historial de mensajes y campo de texto libre. Es honesto: `responderChatIA` reconoce un set de preguntas reales sobre los datos reales de la sesión (quién no responde, qué es urgente, qué vence pronto, resumen del día, seguimientos, "recuérdame...") — **no es un LLM de propósito general** porque este proyecto no tiene ninguna API de IA externa conectada en el backend (todo el motor `emailEngine.js` es JS determinístico); para lo que no reconoce cae en un resumen útil en vez de inventar una respuesta. "Recuérdame X" intenta engancharse a la tarea de la persona que menciona (nuevo seguimiento en `p.seguimientos`) o, si no encuentra a quién, se guarda como recordatorio general (nuevo estado `recordatoriosGenerales`, listado también en Seguimientos).
  - **Pedido explícito para OpenCode**: la pestaña Calendario hoy solo arma su vista con fechas límite/seguimientos derivados localmente — el documento pide integración real con Google Calendar (OAuth + Pub/Sub para sincronía en vivo). Igual que Gmail/Sheets, esa conexión externa cae en su área — cuando puedan, avísenme para wire-earlo desde Calendario en vez de dejarlo como placeholder.
  - Probado con Playwright (claro/oscuro/móvil ~390px), 0 errores de consola/página, `npm run build` limpio, antes de subir. `src/App.css` recibió solo componentes nuevos (KPI cards, banner, chat panel, `.input` genérico) — ningún color/tipografía de la Paleta Aurora de OpenCode fue tocado.

- 2026-09-14 (4) — **Claude**: el Señor envió un mockup exacto del Dashboard (tema verde esmeralda, sidebar oscuro sólido) con la instrucción **"quiero exactamente esto asi!! tal cual, elimina todo lo que sea duplicado"** — esto reemplaza la Paleta Aurora Conectada violeta de OpenCode (13:15) y mi estructura anterior de 4 buckets (09-14 (3)) donde entren en conflicto con el mockup. Cambios:
  - **Nueva paleta verde esmeralda** en `src/index.css` (reemplaza Aurora violeta, mismas variables `--accent`/`--accent-gradient`/etc. así que todo lo que ya usaba `var(--accent)` se retiñe solo) y barrido completo de colores quemados (hex/rgba) en `src/App.css` — verificado con grep que no queda ningún violeta. También corregí `src/components/LoginScreen.css`, que se me había quedado con la paleta violeta vieja en un primer pase (fondo del hero, sombras de la tarjeta, alerta) — ya está en verde consistente.
  - **Sidebar**: ahora es una franja verde bosque sólida oscura (no tarjetas blancas), con exactamente 7 pestañas (Inicio, Bandeja inteligente, Seguimientos, Tareas, Calendario, Contactos, Configuración) — **eliminé duplicados**: la pestaña "Archivados" se fusionó como un filtro más dentro de "Bandeja inteligente" (antes era su propia pestaña separada con su propia lista); "Exportar"/Google Sheets se fusionó como botón dentro de "Tareas" (antes pestaña propia); "Historial"/Auditoría se fusionó dentro de "Configuración" (antes pestaña propia) — ninguna funcionalidad se perdió, solo dejaron de estar duplicadas como pestañas separadas cuando ya vivían naturalmente dentro de otra sección.
  - **Topbar**: rehecho con buscador, pastilla de estado de Gmail, campana de notificaciones y un menú de cuenta (antes era un simple avatar+botones sueltos); el interruptor claro/oscuro y "Sincronizar ahora" que antes eran botones sueltos en el topbar ahora viven dentro de ese menú de cuenta.
  - **Panel "Asistente personal"**: retiré la Mascota flotante y el botón de chat flotante (💬 abajo-izquierda) de la fase anterior — el mockup no los pedía y duplicaban la misma función de conversar con el asistente. En su lugar, una sola columna derecha embebida (solo visible en Inicio) con el mismo chat real de antes (`responderChatIA`, honesto, sin inventar datos) + 4 accesos rápidos + una tarjeta "Tu agenda" (con lo que vence hoy, derivado de datos reales, nunca inventado) + "Seguimientos próximos".
  - **Bandeja inteligente**: creé un componente compartido `renderMailRow` para la fila de correo, usado tanto en la vista previa de Inicio como en la pestaña dedicada — antes había dos implementaciones separadas y ligeramente distintas de la misma fila.
  - Encontré y corregí, durante la verificación con Playwright, un bug real de responsividad: en pantallas de celular (~390px) la clase `.layout.con-asistente` (usada solo en Inicio) le ganaba en especificidad CSS a la regla que colapsa el sidebar a una sola columna, así que el sidebar quedaba apretado en una franja angosta al lado del contenido en vez de apilarse arriba — corregido en `src/App.css` (reglas `@media` de 900px y 720px). También corregí que la tarjeta "Tu asistente te recuerda" no tenía variante de modo oscuro, así que el texto quedaba casi invisible (texto casi blanco sobre fondo casi blanco) en modo oscuro.
  - Probado con Playwright (claro/oscuro/móvil ~390px, las 7 pestañas, el chat del asistente, marcar tarea como "Listo", menú de cuenta) — 0 errores de página, 1 error de consola de red (tunnel bloqueado, no afecta la app). `npm run build` y `tsc` limpios.

- 2026-09-16 — **Claude**: el Señor envió una especificación formal de 58 secciones ("MI ASISTENTE — Especificación visual y UX del Dashboard") con valores exactos (hex de paleta, tipografía, radios, sombras, iconografía) para que la app se ajuste **con fidelidad** al mockup, no solo "parecido". Ajusté `src/index.css`/`src/App.css`/`index.html` (mi área) para que los valores del código coincidan con los de la especificación, sección por sección:
  - **Paleta exacta (sección 38)**: mismas variables de siempre (`--accent`, `--bg`, `--text`, etc., ningún renombre) pero con los valores hex exactos que dio el documento — verde principal `#18875F`, sidebar `#073B3A` (ahora sólido, sin degradado), fondo `#F8FAFC`, texto principal `#17324D` (antes tenía un tinte verdoso, ahora es el gris-azulado que pide el documento), texto secundario `#64748B`, bordes `#E2E8F0`, rojo `#EF4444`, morado `#8B5CF6`, y agregué `--info:#3B82F6` (azul) que no existía como variable. Barrido completo de los `rgba(...)` quemados en `App.css` que aún usaban el verde viejo, para que coincidan con el nuevo tono exacto.
  - **Tipografía (sección 37)**: reemplacé Outfit/Work Sans por **Inter** (cuerpo) + **Plus Jakarta Sans** (títulos/labels en negrita) — las dos que pide el documento — en `index.html` y en todos los `font-family` de `App.css`/`LoginScreen.css`.
  - **Sombras y radios (secciones 40/41)**: las sombras ya no llevan tinte de color de marca — ahora son neutras (`rgba(15,23,42,...)`) y más suaves, como pide el documento ("nunca sombras fuertes"). Los radios de tarjeta bajaron de 18-20px a 16px y los de controles/inputs de 12px a 10px, para igualar la escala exacta que especifica (Cards 14-16 / Controles 8-10).
  - **Iconografía (sección 42, "una sola familia de íconos, lineales, no mezclar estilos")**: esto era la brecha más grande frente al documento — la app usaba emoji sueltos (🏠✉️🔁✅🔔🔎▾🤖 etc.) en vez de íconos lineales de una sola familia. Instalé `lucide-react` (nueva dependencia — **ver nota abajo para quien construya en este equipo**) y reemplacé con íconos Lucide: las 7 pestañas del sidebar, la lupa del buscador, la campana, la flecha del menú de cuenta, el logo (ahora un ícono de chispas/sparkle en vez de las letras "MA", tal como pide la sección 4 y como se ve en el mockup), las 4 tarjetas KPI, los accesos rápidos y el input del Asistente personal, "Tu agenda"/"Seguimientos próximos", "Acciones rápidas" de la Bandeja, y el logo + íconos de features de la pantalla de login. Dejé sin tocar los emoji de estado tipo 🟢/🟡/🔴 (sección 55, el propio documento los usa así) y los de contextos secundarios (Configuración/Historial) para no gastar un esfuerzo desproporcionado en algo que el documento no pedía explícitamente ahí.
  - **Nota importante para quien construya el proyecto en este equipo (OpenCode/Señor)**: agregué `lucide-react` a `dependencies` en `package.json` — hace falta correr `npm install` una vez en este equipo antes del próximo `npm run dev`/`npm run build`, o dará error de módulo no encontrado. Subí `package.json` y `package-lock.json` actualizados junto con el resto.
  - Probado con Playwright (claro/oscuro/móvil ~390px, todas las pestañas, el menú de cuenta, el chat del asistente) — 0 errores de página, 1 error de consola de red esperado (tunnel bloqueado en este sandbox). `npm run build` y `tsc` limpios.

<!-- Agentes: agreguen una línea nueva arriba de esta, con fecha, quién y qué, cada vez que terminen algo importante. -->
